function input(){
	_left = keyboard_check(vk_left)
	_right = keyboard_check(vk_right)
	_up = keyboard_check_pressed(vk_up)
	_kick = keyboard_check_pressed(vk_down)
	_space = keyboard_check_pressed(vk_space)	
}